{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dra-driver-nvme.serviceAccountName" . }}
  namespace: {{ include "dra-driver-nvme.namespace" . }}
  labels:
    {{- include "dra-driver-nvme.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
